# Example Invocations — game-changing-features

## Basic trigger
"What would make our billing system 10x better?"

## Full session with context
"Run a 10x analysis on our dashboard feature. Current state: we have basic analytics
charts, no filtering, and users export to CSV manually. Team of 4, 3-month horizon."

## Explicit strategy request
"What should we build next to make the auth flow game-changing?"

## Mission Control parallel dispatch
"While the implementation agent refactors the API layer, run a 10x strategy
session on the notification system."
